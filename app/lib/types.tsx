export type DropdownItem = {
    name: string;
  }

export type MaqamInfo = {
  name: string;
  icon: string;
  numofsheets: number;
}

export type Recentfile = {
  id: number;
  date: string;
  filename: string;
  author: string;
  filetype: string;
}